<map
  id="map"
  latitude="{{location.latitude}}"
  longitude="{{location.longitude}}"
  scale="{{scale}}"
  setting="{{setting}}"
  markers="{{markers}}"
>
<view class="icon-container">
  <cover-image class="round" 
  src="{{avatarURL? avatarURL: '/resources/account.png'}}" bindtap="onMyTripsTap"></cover-image>
  <cover-image 
  src="/resources/my_location.png"
  bindtap="onMyLocationTap"
  ></cover-image>
</view>
<view class="btn-container bg-grey round" bindtap="onScanTap">扫码租车</view>
</map>